# core/config

Environment & flavor configuration.

- `app_config.dart` — `AppConfig` with `baseUrl`, `env`, `enableLogs` (create when needed).
- Use `--dart-define=ENV=prod` or `flutter_flavor` pattern.

Placeholder — no code yet, directory reserved to avoid import churn later.
